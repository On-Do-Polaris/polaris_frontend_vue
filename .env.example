# 환경 변수 예제
# 개발 시 해당 파일을 '.env' 파일로 복사하여 작업 - (cp .env.example .env)

# API 베이스 URL (상대 경로 - Nginx에서 백엔드로 프록시)
# 배포/운영: /api (Nginx가 polaris-backend-java:8080으로 프록시)
# 로컬 개발: http://localhost:8080 (백엔드를 로컬에서 직접 실행)
VITE_API_BASE_URL=/api

# VWORLD API Key
VITE_VWORLD_API=key1
